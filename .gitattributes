# Shell scripts must use LF for WSL/Linux.
deploy/release/*.sh text eol=lf
deploy/official/*.sh text eol=lf
# Linux 构建在 bash 里逐行 read，CRLF 会让校验路径尾部带 \r（00:21 构建曾因此误报资产缺失）
deploy/official/required-assets.txt text eol=lf
