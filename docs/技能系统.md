# 技能系统（Skills）

> 技能是**面向 LLM 的运行时指令**（一份任务领域指南），不是可执行代码插件。
> Root 侧使用规范文案在 `src/coara/prompts/agents/root.md` §技能使用规范；缓存约束见 [`PROMPT_CACHE_POLICY.md`](./PROMPT_CACHE_POLICY.md)。

---

## 1. 一张图

```text
磁盘（人维护）                        运行时（LLM / Root）
─────────────────                    ──────────────────────────────
skills/（内置）             ┐
<coara_home>/users/default/skills/  ├─► discover → 技能池（SkillManager）
<workspace>/.coara/skills/  ┘            │
  每目录一个 SKILL.md                     ├─ skill(action=search)   → 按关键词查 name + description
  增删 = 放/删目录                        └─ skill(action=activate) → 完整 SKILL.md → message_history
```

**System prompt 只注入技能名清单**（root.md 的 `${COARA_SKILL_LIST}` 占位符，启动时发现、会话内静态；`listed: false` 的技能不进清单，只能经 `skill(action=search)` 发现）。会话内要忘掉已加载的指南 → `/new`。

---

## 2. 磁盘：增删技能

### 目录约定

```text
my-skill/
├── SKILL.md       # 必需：YAML frontmatter（name, description）+ 正文指南
├── references/    # 可选
└── scripts/       # 可选
```

frontmatter 的 `name`、`description` 必填，缺了该技能加载失败（记 warning 跳过）。`name` 保留 Unicode 词字符（中文名原样保留）、下划线、连字符，其他字符**逐个替换为 `_`**。`listed`（默认 `true`）为 `false` 时该技能不进 system prompt 的技能名清单，只能靠 `skill(action=search)` 发现。

### 发现来源（`SkillManager.discover()`，同名后者覆盖前者）

| 顺序 | 路径 | 说明 |
|------|------|------|
| 1 | 仓库 `skills/` | 内置 |
| 2 | `<coara_home>/users/default/skills/` | 用户级（有 coara_home 时） |
| 3 | `<workspace>/.coara/skills/` | 工作空间级（需 `is_trusted`） |
| 4 | 运行时 `extra_paths` | 扩展 |

加载器对每个来源目录递归查找 `SKILL.md`（`rglob`）。

| 操作 | 做法 |
|------|------|
| 添加 | 新建目录 + `SKILL.md` → `/new` 或重启 coara 后生效 |
| 删除 | 删目录 |
| 修改 | 改磁盘文件；history 里已 activate 的旧正文不会自动更新，需 `/new` 后重新 activate |

`/new` 时会重新加载配置并重新 `discover()`（`src/coara/base.py` 的 `start_new_session` 流程），所以增删技能不需要重启进程。

编写规范见 `skills/skill-creator/SKILL.md`。

---

## 3. 运行时：`skill` 工具

仅 **Root**（`owner_only`）。两个 action，**没有 add/remove**——磁盘增删见上节。

| action | 作用 | 改会话状态 |
|--------|------|------------|
| `search` | 在全部技能（含挂起）中按关键词查找，返回候选 name + description（只看不用）；query 留空返回全量目录 | 否 |
| `activate` | 把完整 SKILL.md 以 `<系统提醒>` + `<activated_skill>` 形式写进 history | 是 |

推荐流程（每步仅在本会话尚未完成时执行）：

1. `skill(action="search")`
2. 选最匹配的技能
3. `skill(action="activate", name=…)`

search 输出里的标记：

- `挂起` — frontmatter `listed: false`，不进 system prompt 技能名清单，只能靠 search 发现再 activate
- `activated` — 本会话已 activate

`activate` 的内部行为（`src/skills/activation.py`）：

- 名字不存在 → 返回 error 并附上可用技能清单；
- 成功 → 技能名加入 `SkillSessionState.activated`，content 为 `<activated_skill name=…>`（含正文、磁盘路径、可选 references 列表）；
- `workflow` 技能额外置 `_workflow_draft_pending`：本 turn 内若 LLM 没成功调 `orchestrator(action="save/run")` 就想结束，turn 循环会注入续跑提醒。

`SkillSessionState`（`src/skills/session.py`）只记 `activated: set[str]`，`/new` 时 `clear()`。

---

## 4. 推荐配置（可选）

```yaml
# config.yaml
skills:
  default_include: []   # Dashboard 推荐勾选（/api/v1/skills）
  default_exclude: []
```

推荐标记（现行实现）：Dashboard / `/api/v1/skills` **直读** `config.yaml` 的 `skills.default_include`（`["*"]` 表示全选）算出推荐集合；agent YAML 的 `skills.include` / `skills.exclude` 是另一用途（决定该 agent 装载哪些技能，见 `src/prompt/yaml_loader.py`），不参与该推荐标记。这套配置**只决定 Dashboard 推荐勾选**，不改变 discover 结果，`skill(action=search)` 也不输出 default 标记。

---

## 5. Root 规范（摘要）

见 `root.md` §技能使用规范：

- 领域任务优先用技能完成，不凭通用经验直接做；
- 不确定用哪个技能时先 `skill(action="search")` 查候选，确定后 `skill(action="activate", name=…)` 加载（name 取唯一技能名）；
- 已加载过则无需重复；加载的技能必须严格遵照执行。

---

## 6. 内置技能（仓库 `skills/`）

| 目录 | 用途一句话 |
|------|-----------|
| `event-source` | 事件源配置（kind / salience / handle / 模板；规格见同目录 EVENT_SOURCE_SPEC） |
| `research` | 多源调研与结构化报告 |
| `工作空间管理` | coara Home 侧配置与工作空间运维 |
| `skill-creator` | 怎么写新技能 |
| `tool-creator` | 怎么写磁盘工具包 |
| `create-rule` | 编写 `ws.md` / `AGENTS.md` / `user_rules.md` |
| `self_demo` | 自演示 |

编排用挂起工具 `orchestrator`（`tool(activate)`）；图/视频用内置 `media`。Agnes 文本聊天可走 `agnes` provider（见 [CONFIGURATION.md §3.0](CONFIGURATION.md#30-agnes-endpoint)）。

以运行时 `skill(action="search")` 为准。

---

## 7. 源码

| 文件 | 职责 |
|------|------|
| `src/skills/manager.py` | discover / get / get_all（每个 CoaraBase 持独立实例 `self.skill_manager`；Web 设置页列表走 `control_plane` 的独立只读实例） |
| `src/skills/loader.py` | 解析 SKILL.md（frontmatter + name 清洗） |
| `src/skills/activation.py` | activate → ToolResult（`<activated_skill>` 组装） |
| `src/skills/session.py` | `SkillSessionState.activated` |
| `src/ui/dashboard_handlers.py` | `/api/v1/skills` 直读 `config.yaml` 的 `skills.default_include`（`["*"]`＝全选）算推荐标记 |
| `src/tools/builtin/skills/skills.py` | `SkillTool`（search / activate） |
| `src/coara/base.py` | `load_skills()`、`/new` 时的重发现 |

---

## 8. 速查

| 我想… | 做法 |
|-------|------|
| 加 / 删技能 | 磁盘放 / 删含 `SKILL.md` 的目录，然后 `/new` |
| 让 agent 用技能 | `search`（如需）→ `activate` |
| 忘掉已加载指南 | `/new` |
| 推荐技能到 Dashboard | `config.yaml` 的 `skills.default_include` 或 Dashboard |
| 写 SKILL.md | activate `skill-creator` 或读它的 SKILL.md |

```bash
pytest tests/test_skills/ -q
```
