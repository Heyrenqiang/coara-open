# examples/ — 示例工作空间安装

逻辑在 `src/examples/`；已安装 coara 时**优先用 CLI**。

```bash
coara examples install stocks-watch
```

直接跑脚本（预览）：

```bash
python scripts/examples/install_stocks_watch.py --dry-run
```

写入 coara Home 并注册 `@stocks-watch`（配置来自 `examples/stocks-watch/`）。
