"""
coara v8 - Core Runtime Module

Package entry re-exports are intentionally omitted here to avoid import cycles
when loading submodules (e.g. ``coara.injections.background_injector`` from skills/agent code).
Import ``CoaraBase`` / ``RootCoara`` from ``src.coara.base`` / ``src.coara.root``.
"""
